// Destination for every "Apply Today" button (hero, header, mobile menu, closing sections).
// Until the client provides an application link (e.g. a careers or applicant-tracking URL),
// applicants email HR directly. Replace this value with that link when it arrives. Never point
// it at /contact, which is the business inquiry form. An empty string hides the Apply buttons.
export const APPLY_URL = 'mailto:hr@abcgroupatl.com?subject=Application';
